#!/usr/bin/env sh

# mission-control desktop labels
yabai -m space 1 --label todo
yabai -m space 2 --label web
yabai -m space 3 --label code
yabai -m space 4 --label terminal
yabai -m space 5 --label finder
yabai -m space 6 --label chat
yabai -m space 7 --label music
yabai -m space 8 --label misc1
yabai -m space 9 --label misc2
yabai -m space 10 --label notes

# window rules
yabai -m rule --add app="^Mail$" space=chat
yabai -m rule --add app="^Safari$" space=web
yabai -m rule --add app="^Notes$" space=notes
yabai -m rule --add app="^Spotify$" space=music
yabai -m rule --add app="^Discord$" space=chat grid=1:1:0:0:1:1 manage=off
yabai -m rule --add app="^(Calculator|System Preferences|Archive Utility|Aqua Voice)$" manage=off
yabai -m rule --add app="^mpv$" manage=off border=off sticky=on layer=above opacity=1.0 grid=8:8:6:0:2:2

# signals
yabai -m signal --add event=window_destroyed active=yes action="yabai -m query --windows --window &> /dev/null || yabai -m window --focus mouse &> /dev/null || yabai -m window --focus \$(yabai -m query --windows --space | jq .[0].id) &> /dev/null"
yabai -m signal --add event=window_minimized active=yes action="if \$(yabai -m query --windows --window \$YABAI_WINDOW_ID | jq -r '.\"is-floating\"'); then yabai -m query --windows --window &> /dev/null || yabai -m window --focus mouse &> /dev/null || yabai -m window --focus \$(yabai -m query --windows --space | jq .[0].id) &> /dev/null; fi"

# ghostty: re-apply bsp layout to work around native macOS tabs appearing as separate windows
# see: https://ghostty.org/docs/help/macos-tiling-wms + yabai#68
yabai -m signal --add app='^Ghostty$' event=window_created action='yabai -m space --layout bsp'
yabai -m signal --add app='^Ghostty$' event=window_destroyed action='yabai -m space --layout bsp'
yabai -m signal --add app='^Ghostty$' event=window_moved action='yabai -m space --layout bsp'
yabai -m signal --add app='^Ghostty$' event=window_resized action='yabai -m space --layout bsp'

# global settings
yabai -m config                              \
    external_bar                main:32:0    \
    mouse_follows_focus         on           \
    focus_follows_mouse         autoraise    \
    window_placement            second_child \
    window_topmost              off          \
    window_shadow               off          \
    window_animation_duration   0.25         \
    split_ratio                 0.50         \
    split_type                  auto         \
    auto_balance                off          \
    top_padding                 10           \
    left_padding                10           \
    right_padding               10           \
    bottom_padding              10           \
    window_gap                  10           \
    layout                      bsp          \
    mouse_modifier              fn           \
    mouse_action1               move         \
    mouse_action2               resize       \
    mouse_drop_action           swap

echo "yabai configuration loaded.."
