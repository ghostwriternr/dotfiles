#!/bin/bash

source ~/.config/lemonbar/fixtop

{
# zscroll -l 20 -b "%{l}      " -u "mpc current -p 6601" &
while true; do
    echo "%{lO600}$(count) \
        %{r}$(spotify_control)    "
    sleep 1s
done
} |

lemonbar\
    eDP1 \
    -g "1750x28+170+0"\
    -F "#ffffff"\
    -B "#00000000"\
    -f "$PANEL_FONT_FAMILY"\
    -d \
    -f "$ICON_FONT" | bash
